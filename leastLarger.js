function leastLarger(array,index) {
  
    let checkIndex = array.findIndex(index);
    console.log(checkIndex);
    // for(let i = 0; i < array.length; i++){
    //     if(indexOf(index)){

    //     }
    // }
    // return -1;
  }
  leastLarger([4, 1, 3, 5, 6], 0);//  =>  3
// leastLarger([4, 1, 3, 5, 6], 4);//  => -1